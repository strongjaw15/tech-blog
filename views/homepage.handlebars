<section>
  {{#each blogPosts}}
    <div class="post">
      <a href="/api/blogpost/{{id}}">
        <h2>{{title}}</h2>
      </a>
      <h3>{{user.username}}</h3>
      <p>
        {{content}}
      </p>
    </div>
  {{/each}}
</section>